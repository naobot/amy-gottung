backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'home'
    label: 'Home'
    editor:
      preview: false
    files:
      - label: 'Featured Works'
        name: 'featured'
        file: '_data/featured.yml'
        fields:
          - label: 'Currently Featured Works'
            name: 'items'
            widget: 'list'
            fields:
              - { label: Work, name: work_title, widget: 'relation', collection: 'works', search_fields: ['title'], value_field: 'title', display_fields: ['title'] }
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'post' }
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Body', name: 'body', widget: 'markdown' }
  - name: 'works'
    label: 'Work'
    folder: '_works/'
    create: true
    slug: '{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'work-item' }
      - { label: 'Title', name: 'title', widget: 'string', hint: 'Please ensure this is unique' }
      - { label: 'Client', name: 'client', widget: 'string', required: false }
      - { label: 'Client Link', name: 'client_link', widget: 'string', required: false, pattern: ['https?:\/\/[\w\d]+\.[\w\d]+\.?[\w\d]*\/?.*', 'Must be a valid URL (begins with http://)'] }
      - { label: 'Role', name: 'role', widget: 'string', required: false }
      - { label: 'When', name: 'when', widget: 'string', required: false }
      - { label: 'Where', name: 'where', widget: 'markdown', minimal: true, buttons: ['link'], editor_components: [], required: false }
      - { label: 'Team', name: 'team', widget: 'markdown', minimal: true, buttons: ['link'], editor_components: [], required: false }
      - { label: 'Partners', name: 'partners', widget: 'markdown', minimal: true, buttons: ['link'], editor_components: [], required: false }
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - { label: 'Description', name: 'body', widget: 'markdown' }
      - { label: "Category", name: "categories", widget: "select", options: ["creative", "consulting", "speaking"] }
      - { label: "Tags", name: "tags", widget: "list" }
      - { label: "Main Image", name: "thumbnail", widget: "image", allow_multiple: false }